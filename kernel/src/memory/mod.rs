use limine::response::MemoryMapResponse;

pub fn init(_mmap: &MemoryMapResponse, _hhdm_offset: u64) {
    // Stub — implemented in Phase 02
    // Phase 01 just needs this to exist so main.rs compiles
}
